/* shrike/window.cpp — the Shrike Window object (S0.1 skeleton).
 *
 * S0.1: inert shell. S0.2/S0.3 attach the X11 window (create/map,
 * event dispatch); S0.4 gives it the text path. Keeping the class here
 * from the start means the shared-library ABI and the build rules are
 * exercised by the S0.1 skeleton before any X code exists.
 */
#include <shrike/shrike.h>

namespace shrike {

Window::Window()
{
}

Window::~Window()
{
}

} /* namespace shrike */
